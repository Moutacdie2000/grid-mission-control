{
  "name": "@grid/server",
  "private": true,
  "version": "1.0.0",
  "description": "GRID — backend d'observabilité temps réel (API REST + flux SSE).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10",
    "@fastify/static": "^8",
    "fastify": "^5"
  }
}
